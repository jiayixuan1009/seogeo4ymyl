server {
    listen 80;
    server_name seo.cryptooptiontool.com;

    root /var/www/seotool;
    index index.html;

    # For SPA (Vite/React/etc) history fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
